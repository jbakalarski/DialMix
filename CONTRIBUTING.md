# Contributing

DialMix is open source and welcomes focused pull requests. Keep Windows Core Audio access behind interfaces, keep OpenDeck protocol code separate from the audio engine, and add tests for pure logic. All documentation and commit messages are written in English.

Run `dotnet build`, `npm test`, and `npm run package` before opening a pull request. Do not commit generated `bin`, `obj`, `dist`, or plugin package files.
