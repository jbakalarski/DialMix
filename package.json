{
  "name": "dialmix",
  "private": true,
  "description": "Packaging tools for the DialMix OpenDeck plugin",
  "scripts": { "package": "node scripts/package-plugin.js", "test": "node --test scripts/*.test.js" }
}
